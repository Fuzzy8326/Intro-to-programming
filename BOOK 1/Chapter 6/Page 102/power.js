// Initialize the starting power of 3
let power = 3;

// Continue looping while power is less than 10,000
while (power < 10000) {
  // Print the current value of power
  console.log(power);

  // Multiply power by 3 to get the next power of 3
  power *= 3;
}
