// Define a function named 'result' that takes two parameters and returns their sum
function result(a, b) {
  return a + b;
}

// Call the 'result' function with the product of 36325 and 9824 as the first argument,
// and 777 as the second argument. Store the final sum in the variable 'total'.
var total = result(36325 * 9824, 777);

// Output the value of 'total' to the console
console.log("The result is: " + total);
